on:
  workflow_dispatch:

--- a/.github/workflows/test-minimal.yml
+++ b/.github/workflows/test-minimal.yml
@@ -45,4 +45,4 @@
         run: make -C ./docs html SPHINXOPTS="-E -W --keep-going"
 
       - name: Test code
-        run: pytest --verbose --maxfail=1 --typeguard-packages=osmnx
+        run: pytest --verbose --maxfail=1 --typeguard-packages=osmnx

# bfp-replay-registration: e1c76aeb2a48-llm
